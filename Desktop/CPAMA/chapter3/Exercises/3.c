#include <stdio.h>

int main() {
    int x,y,z,a,s,d;
    //    double q, w, e, r, t, y; 
    scanf("%d-%d-%d", &x, &y, &z);
    scanf("%d -%d -%d", &a, &s, &d);
    printf("%d%d%d\n", x, y, z);
    printf("%d%d%d\n", a, s, d);

    return 0;
} 
